# OpenAI API — server-side receipt OCR (vision). Never commit real keys.
OPENAI_API_KEY=

# Next.js app (apps/next-web) — required by lib/env.ts
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: server routes that use Supabase service role
# SUPABASE_SERVICE_ROLE_KEY=
