# Claude API (TRD §2 — OCR + AI summaries). Never commit real keys.
ANTHROPIC_API_KEY=

# Optional: encrypted backup (TRD §2)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# Dev server (web / Tauri)
# Vite loads env from the app directory (apps/web) when using npm workspaces from repo root.
