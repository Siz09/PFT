[build]
# Move Cargo output outside the project root so Metro's file watcher
# doesn't crash on Rust temp files during compilation.
# Resolves to D:\vscode\smartspend-target (sibling of the PFT project dir).
target-dir = "../../smartspend-target"
