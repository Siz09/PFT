{
  "name": "smartspend-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:web": "npm --prefix apps/next-web run dev",
    "lint:web": "npm --prefix apps/next-web run lint",
    "build:web": "npm --prefix apps/next-web run build"
  }
}
